# Metadata

This directory stores lightweight, version-controlled metadata required to validate and interpret the environmental catalog.

## Contents

- `schemas/` — machine-readable contracts for catalog tables.

Provider-specific licences, variable dictionaries, provenance templates, or schema extensions should be added here when the corresponding environmental family is reviewed.

Repository identity belongs in the root-level `repo.yml`; it is intentionally not duplicated inside `metadata/`.
